---
description: Compila o app (erros TypeScript + Vite), reporta resultado.
---

# Build Tecladista IA

1. Na raiz do repo: `npm run build` (typecheck TS strict + build Vite)
2. Reportar resultado: ✅ sucesso ou ❌ falha com lista de erros (arquivo, linha, mensagem)
