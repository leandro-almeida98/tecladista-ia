---
description: Executa testes unitários + cobertura e reporta resultado por arquivo.
---

# Testes Tecladista IA

1. Na raiz do repo: `npm test` (Vitest run — suíte unitária)
2. Na raiz do repo: `npm run test:coverage` (cobertura Vitest v8)
3. Reportar: ✅ todos passam ou ❌ lista de falhas; cobertura POR ARQUIVO (meta 95%) destacando arquivos abaixo do limite
